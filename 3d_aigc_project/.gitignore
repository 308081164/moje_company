# 环境变量（保留 .env.example）
.env
.env.local
.env.*.local

# HuggingFace / 下载缓存
.cache/
**/.cache/

# 模型权重（体积大，见 docs/模型下载指南.md）
models/*
!models/.gitkeep
ai-service/models/*
!ai-service/models/.gitkeep

# 运行时产物
outputs/
uploads/

# Node / 前端构建
node_modules/
dist/

# Java / Maven
target/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 本地开发工具链（JDK/Maven/Node 便携包）
.tools/
