# Vite 开发模式（配合 docker-compose.dev.yml）
FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm config set registry https://registry.npmmirror.com \
    && npm ci

COPY . .

EXPOSE 8853

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "8853"]
