# ============================================================
# 3D AIGC 推理服务 - Python依赖
# ============================================================

# Web框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Pydantic数据验证
pydantic>=2.5.0

# 深度学习框架（torch/torchvision 由 Dockerfile 从 cu121 索引单独安装，勿在此重复）

# Hunyuan3D-2 3D生成模型
hy3dgen>=0.1.0

# 3D处理库
trimesh>=4.0.0
rtree>=1.0.0
open3d>=0.18.0

# 数值计算
numpy>=1.24.0

# 图像处理
Pillow>=10.0.0
opencv-python-headless>=4.8.0
rembg>=2.0.50
onnxruntime>=1.16.0

# SAM 点选宝石分割（可选，见 scripts/download-sam-model.ps1）
segment-anything>=1.0
# SAM2（优先）: pip install git+https://github.com/facebookresearch/sam2.git

# 布尔运算（可选，提升融合质量）
manifold3d>=2.0.0

# 网格水密修复（marching cubes / 平滑后补洞）
pymeshfix>=0.16.0

# Ultra CAD 逆向（Open CASCADE / STEP 导出，仅 Linux/Docker）
pythonocc-core>=7.7.0,<7.9.0; sys_platform == 'linux'

# Ultra/Quality mc_algo=dmc（DiffDMC，Dockerfile ENABLE_DMC=1 时单独编译安装）
# diso==0.1.4

# 宝石去反光 AI 重绘（InstructPix2Pix）
diffusers>=0.27.0
transformers>=4.38.0
accelerate>=0.27.0
safetensors>=0.4.0

# HTTP客户端（用于回调通知）
httpx>=0.25.0

# 环境变量管理
python-dotenv>=1.0.0
